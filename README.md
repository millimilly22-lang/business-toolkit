# Business Toolkit

Website-first business workspace for invoices, receipts, expenses, document capture and spreadsheet analysis.

## Deploy on Render
Create a **Static Site**, connect this repository, leave the build command empty, and use `.` as the publish directory.

The app works in the browser without an API key or backend. Core records are stored locally in the browser.
